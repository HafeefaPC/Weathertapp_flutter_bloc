name: flutter_weather
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter  
  flutter_bloc: ^8.1.6
  http: ^1.2.1
  equatable: ^2.0.5
  weather: ^3.1.1
  intl: ^0.19.0
  geolocator: ^12.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/